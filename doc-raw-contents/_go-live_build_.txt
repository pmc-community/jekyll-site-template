building and testing the site means go generate the site, running the automatic testing and the spell checking. following the site build and tests results, corrections may be needed. when everything is fine, is time to serve the site locally and test its appearance and functionality. docaroo provides a ready-made script for building and running automatic tests on the development environment. this script is optimised for macos and linux and can be started with command. in a similar way, on windows machines, can be used. to build the site: open a terminal window navigate to the root directory of the site run preflight test spell check